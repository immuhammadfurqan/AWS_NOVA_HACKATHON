# =============================================================================
# AARLP - AI-Agentic Recruitment Lifecycle Platform
# Environment Configuration File
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control (it's in .gitignore)
# =============================================================================

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================
APP_NAME=AARLP
DEBUG=false
TIMEZONE=UTC

# =============================================================================
# AUTHENTICATION (REQUIRED)
# =============================================================================
# Generate a secure secret key: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or use: openssl rand -hex 32
SECRET_KEY=your-secret-key-at-least-32-characters-long-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# DATABASE (PostgreSQL + pgvector)
# =============================================================================
# Format: postgresql+asyncpg://username:password@host:port/database
# Make sure PostgreSQL has pgvector extension installed
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/aarlp

# =============================================================================
# AI PROVIDER SELECTION
# =============================================================================
# Choose: "openai" or "bedrock" (AWS Bedrock Nova models)
# Default: "bedrock" (for AWS Nova hackathon)
AI_PROVIDER=bedrock

# =============================================================================
# AWS BEDROCK (Nova Models) - REQUIRED if AI_PROVIDER=bedrock
# =============================================================================
# Get these from AWS Console -> IAM -> Users -> Security Credentials
# Or use AWS CLI: aws configure
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1

# Bedrock Model IDs (Nova models)
BEDROCK_MODEL_ID=amazon.nova-lite-v1:0
# Options: amazon.nova-lite-v1:0, amazon.nova-pro-v1:0, amazon.nova-premier-v1:0

# Embedding Model (Titan)
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0
BEDROCK_EMBEDDING_DIMENSION=1024

# =============================================================================
# OPENAI (Fallback) - REQUIRED if AI_PROVIDER=openai
# =============================================================================
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EMBEDDING_DIMENSION=1536

# =============================================================================
# PINECONE (Vector Database) - REQUIRED
# =============================================================================
# Get API key from: https://app.pinecone.io/
# Create an index with dimension matching your embedding model:
# - 1024 for AWS Bedrock Nova/Titan embeddings
# - 1536 for OpenAI text-embedding-3-small
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX=aarlp-candidates
PINECONE_INDEX_NOVA=aarlp-nova-candidates

# =============================================================================
# VOICE AI PROVIDER SELECTION
# =============================================================================
# Choose: "twilio" or "elevenlabs"
VOICE_PROVIDER=twilio

# =============================================================================
# TWILIO (Voice Calls) - REQUIRED if VOICE_PROVIDER=twilio
# =============================================================================
# Get credentials from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# =============================================================================
# ELEVENLABS (Text-to-Speech Alternative) - REQUIRED if VOICE_PROVIDER=elevenlabs
# =============================================================================
# Get API key from: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your-elevenlabs-api-key
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# =============================================================================
# EMAIL / SMTP (For OTP and Notifications) - REQUIRED
# =============================================================================
# Gmail example (requires App Password):
# - Enable 2FA on your Google account
# - Generate App Password: https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-or-smtp-password
MAIL_FROM=noreply@aarlp.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_STARTTLS=true
MAIL_SSL_TLS=false
USE_CREDENTIALS=true
VALIDATE_CERTS=true

# Other SMTP providers:
# - Outlook: smtp-mail.outlook.com:587
# - SendGrid: smtp.sendgrid.net:587 (username: apikey, password: your-api-key)
# - AWS SES: email-smtp.us-east-1.amazonaws.com:587

# =============================================================================
# GOOGLE CALENDAR (Interview Scheduling) - OPTIONAL
# =============================================================================
# Download credentials.json from Google Cloud Console:
# 1. Go to: https://console.cloud.google.com/
# 2. Create a project and enable Google Calendar API
# 3. Create OAuth 2.0 credentials (Desktop app)
# 4. Download as credentials.json
GOOGLE_CALENDAR_CREDENTIALS_FILE=credentials.json
GOOGLE_CALENDAR_TOKEN_FILE=token.json

# =============================================================================
# REDIS (Distributed Locking & OTP Storage) - REQUIRED
# =============================================================================
# Install Redis: https://redis.io/download
# Or use Docker: docker run -d -p 6379:6379 redis:alpine
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# For production (Redis Cloud, AWS ElastiCache, etc.):
# REDIS_HOST=your-redis-host.redis.cache.windows.net
# REDIS_PORT=6380
# REDIS_PASSWORD=your-redis-password

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================
# Frontend URLs that can access the API
# Add your production frontend URL when deploying
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# NOTE: Application tuning parameters (LLM_TEMPERATURE, MIN_APPLICANT_THRESHOLD, etc.)
# are configured in app/core/config.py with sensible defaults.
# Override them here only if you need different values for your environment.
# =============================================================================

# =============================================================================
# QUICK START GUIDE
# =============================================================================
# Minimum required for local development:
# 1. SECRET_KEY (or use default dev key)
# 2. DATABASE_URL (PostgreSQL with pgvector)
# 3. PINECONE_API_KEY (for vector storage)
# 4. Either AWS credentials (for Bedrock) OR OPENAI_API_KEY
# 5. REDIS_HOST and REDIS_PORT (for OTP and locking)
# 6. MAIL_USERNAME and MAIL_PASSWORD (for email OTP)
#
# For production, set all required values and use strong SECRET_KEY
# =============================================================================
